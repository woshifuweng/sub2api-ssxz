# Upstream 0.1.159 Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the 13 upstream commits ending at `18a2c43ebf1d78025649501c2752977bf7a3654f` without losing SSXZ white-label, security, billing, authentication, or UI work.

**Architecture:** Perform a three-way merge on the isolated `merge/upstream-0.1.159` worktree. Resolve source files by semantic comparison, regenerate Ent/Wire output from resolved sources, then validate the entire backend/frontend and deploy only to staging.

**Tech Stack:** Go 1.26.x, Gin, Ent, Wire, Vue 3, TypeScript, pnpm/Vitest/Vite.

---

### Task 1: Freeze the pre-merge state

**Files:**
- Create outside repository: `C:/Users/24091/Documents/中转站项目/backups/upstream-0.1.159-before-merge-<timestamp>/`

- [ ] Record branch, HEAD, upstream SHA, status, ahead/behind counts, and commit lists.
- [ ] Create a Git bundle containing integration HEAD and `upstream/main`.
- [ ] Verify the integration worktree has no tracked or untracked changes.

### Task 2: Run and inventory the merge

**Files:**
- Modify: all files selected by `git merge --no-commit --no-ff upstream/main`

- [ ] Start the merge without committing.
- [ ] Export the exact unmerged-file inventory grouped as add/add, modify/delete, generated, and ordinary content conflicts.
- [ ] Scan touched paths for authentication, billing, affiliate, provider routing, encryption, migrations, and configuration semantics.

### Task 3: Resolve source-of-truth conflicts

**Files:**
- Modify: `backend/ent/schema/*.go`
- Modify: `backend/cmd/server/wire.go`
- Modify: conflicting backend middleware, services, repositories, handlers, DTOs, and frontend admin/user components.

- [ ] Resolve schema and dependency-injection sources first.
- [ ] For every add/add file, compare base absence, ours, upstream, tests, callers, and related commits; record the selected or fused behavior and reason.
- [ ] Determine whether deleted `backend/cmd/server/coordinator_app.go` has a live replacement and either preserve deletion or restore upstream semantics in the replacement.
- [ ] Preserve SSXZ safety gates and UI while absorbing upstream bug fixes.
- [ ] Stop and report rather than silently changing billing, authentication settings, affiliate ledger semantics, encryption, or provider-routing policy.

### Task 4: Regenerate generated code

**Files:**
- Regenerate: `backend/ent/**`
- Regenerate: `backend/cmd/server/wire_gen.go`

- [ ] Run the repository's Ent generation command from resolved schemas.
- [ ] Run Wire generation from resolved `wire.go`.
- [ ] Confirm no generated file contains conflict markers or manual-only edits.

### Task 5: Validate the merged tree

**Files:**
- Test only.

- [ ] Run `go build ./...` and `go test ./...`.
- [ ] Run frontend `pnpm run typecheck`, `pnpm run lint`, `pnpm run build`, and full `pnpm exec vitest run`.
- [ ] Scan for conflict markers, secrets, forbidden production changes, and unintended white-label regressions.
- [ ] Commit the completed merge and record the merge SHA.

### Task 6: Stage-only deployment gate

**Files:**
- Create server snapshot under `/opt/sub2api/backups/`.

- [ ] Build an embedded Linux binary with the merge SHA.
- [ ] Confirm `rust.sidecar.responses_ws_enabled=false` and no true environment override.
- [ ] Snapshot current staging binary/config/systemd metadata and create rollback material.
- [ ] Deploy only to `sub2api-staging`, then verify critical pages are 200, no-key `/v1/models` is 401, service is active, and `NRestarts=0`.
- [ ] Do not replace or restart the production binary/service.

### Task 7: Publish the merge report

**Files:**
- Modify: `C:/Users/24091/Documents/中转站项目/docs/AI协作/CODEX_TO_CLAUDE.md`

- [ ] Record all add/add decisions, coordinator deletion decision, protected-domain audit, tests/counts, snapshot paths, branch/SHA, and staging evidence.
- [ ] Request Claude review and explicit user decision before any production action.
