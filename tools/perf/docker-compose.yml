services:
  k6:
    image: grafana/k6:0.49.0
    working_dir: /scripts
    environment:
      TARGET_URL: ${TARGET_URL:-http://host.docker.internal:8080}
      API_KEY: ${API_KEY:-}
    volumes:
      - .:/scripts:ro
