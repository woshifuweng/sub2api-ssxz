# 分组10 单点消除 + 定时拨测 运维手册

分支 `ops/group10-redundancy`。针对生产站（SSH 别名 `ssxz-server`，库 `sub2api`）。
状态：**已就绪但未执行** —— 管理 API 被合规承诺闸门锁住，需运营者本人先行确认（见「执行前置」）。

## 1. 问题诊断

分组10「GPT Pro池」近 7 天 `ops_error_logs` 聚合：

| error_phase | 错误 | 模型 | 次数 |
|---|---|---|---|
| internal | No available upstream account supports the requested model | gpt-5.5 | 234 |
| internal | 同上 | gpt-5.6-sol | 202 |
| internal | 同上 | gpt-5.6-luna | 164 |
| request | insufficient balance | gpt-5.5 / gpt-5.6-terra | 42 |
| upstream | 502/503/forbidden 等上游抖动 | 各型 | 11 |

组内账号的模型白名单（`accounts.credentials.model_mapping` 非空即为严格白名单，
见 `backend/internal/service/account.go:907`）：

| 账号 | 名称 | 覆盖模型 |
|---|---|---|
| 34 | Codex Pro | codex-auto-review, gpt-5.5, gpt-5.6, gpt-5.6-luna, gpt-5.6-sol, gpt-5.6-terra（**无 gpt-5.4**）|
| 42 | GPT Pro-StrategyHub-20260722 | gpt-5.4, gpt-5.4-mini, gpt-5.5, gpt-5.6-luna, gpt-5.6-sol, gpt-5.6-terra（**无裸 gpt-5.6**）|

- **gpt-5.4 / gpt-5.4-mini 仅账号42 覆盖** → 42 冷却即整型不可用。
- 600 次 `internal` 错误的模型（5.5 / 5.6-sol / 5.6-luna）两账号都覆盖，
  所以耗尽发生在两账号**同时**被调度器否决时（`runtime_blocked` / `quota_auto_pause` /
  限流窗口，见 `openai_account_scheduler.go:1762`），并非 42 本身故障——**不摘 42**。

## 2. 方案：账号39 作为第三腿（严格待命）

选账号39「Codex-211b」：覆盖 gpt-5.4, gpt-5.4-mini, gpt-5.5, gpt-5.6, gpt-5.6-luna,
gpt-5.6-sol, gpt-5.6-terra —— 是 34 ∪ 42 的**超集**，且上游（211b）与 StrategyHub
故障域分离。它是全库唯一可选：其余 openai 账号或已在组10（34/42）、或 status=error
（30/35）、或属 Claude 系分组（32/37/38/40/43/44）。

**待命而非分流**：`accounts.priority` 升到 50（34/42 均为 1，数值小者优先）。
前置条件已核实成立——`openai_advanced_scheduler_enabled=false`（传统调度）且
`openai_low_upstream_rate_priority_enabled` 未设置（代码要求字面 `"true"` 才生效，
`openai_account_scheduler.go:1898`）。若哪天打开「低费率优先」，
`openai_gateway_scheduling.go:1054-1058` 会按上游费率重排并**覆盖优先级分层**，
待命语义失效，需重新评估。

副作用：`accounts.priority` 是全局列，组15 里 39 也会降为低优先级。当前组15 仅 39 可用
（30/35 为 error），无实际影响；若 30/35 日后恢复，39 在组15 亦变为备份。

## 3. 拨测计划

`scheduled_test_plans` **只能按账号定向，无分组字段**（`migrations/066_add_scheduled_test_tables.sql:4`），
故按组10 成员逐账号建计划，模型取各自白名单内的恒等映射项：

| 账号 | 模型 | cron | 理由 |
|---|---|---|---|
| 42 | gpt-5.4 | `*/30 * * * *` | 改动前唯一 gpt-5.4 承载者 |
| 39 | gpt-5.4 | `*/30 * * * *` | 新备份腿，验证可服务关键模型 |
| 34 | gpt-5.5 | `*/30 * * * *` | 34 不映射 gpt-5.4；5.5 为组10 最大流量 |

- `auto_recover=false`（保守）：拨测只做观测，不自动清账号 error/限流状态。
  若要让成功探测自动解冷却，改为 `true` 即可（单字段 PUT，可逆）。
- cron 为 5 段式（robfig，无秒位、不支持 `@every`），`scheduled_test_service.go:11`。
- 三账号均为 `type=apikey`，探测仅发 `"hi"`，开销极小（OAuth 账号才会附带 ~6.7KB Codex 提示词）。
- runner 生效条件已核实：生产 unit 的 EnvFile 未设置任何
  `BACKGROUND_JOBS_ENABLED/SCHEDULERS_ENABLED/APP_RUNTIME_ROLE/SUB2API_PROCESS_ROLE`
  关闭标志（那些只在 staging.env 里），且日志有 `ScheduledTestRunner` 活动记录。
- 失败无任何告警/邮件/webhook；迁移 191 的自动暂停列是死代码。只能查
  「账号管理 → 三点菜单 → 定时测试 → 测试结果」或 `scheduled_test_results` 表。

## 4. 执行前置（阻塞项）

管理 API 全部写操作返回 **423 `ADMIN_COMPLIANCE_ACK_REQUIRED`**：
`middleware.AdminComplianceGuard` 要求当前管理员存在合规承诺记录
（`settings` 表 `admin_compliance_acknowledgement:1`），生产库该记录**不存在**。
前端无对应界面（`frontend/` 全库无 compliance 相关代码），只能调 API 接受。

该承诺是需运营者本人逐字确认的法律声明（记录 admin_user_id / IP / User-Agent 作为
其电子确认），文档见 `docs/legal/admin-compliance.zh.md`。**代理接受不在授权范围内。**

## 5. 执行与回滚

服务器上已就绪 `/root/ops-g10-apply.py`（600 root:root）：合规未确认时拒绝执行并退出 3
（已实测，未产生任何变更）。确认后 `python3 /root/ops-g10-apply.py` 一次完成绑定 +
建计划 + 校验，并落盘 `/root/ops-group10-pre-<ts>.json` 与 `/root/ops-group10-rollback-<ts>.txt`。

变更前快照：`/root/ops-group10-pre-20260727-035642.sql`（600 root:root）。

回滚（优先走 API）：

```
PUT /api/v1/admin/accounts/39   {"group_ids": [15], "priority": 1}
DELETE /api/v1/admin/scheduled-test-plans/<id>   # 三条
```

兜底 SQL（仅在管理 API 不可用时）：

```sql
DELETE FROM account_groups WHERE account_id = 39 AND group_id = 10;
UPDATE accounts SET priority = 1 WHERE id = 39;
DELETE FROM scheduled_test_plans WHERE id IN (<三个 id>);
```

之后清鉴权缓存：`redis-cli --scan --pattern 'apikey:auth:*' | xargs -r redis-cli DEL`

## 6. 调用绑定接口的两个致命坑

`PUT /api/v1/admin/accounts/:id` 是**全量替换**绑定集（`account_repo.go:1760` BindGroups
先删后建），因此：

1. **只发 `group_ids`（必要时加 `priority`），绝不带 `credentials` 或 `extra`**。
   `MergePreservingSensitiveCreds`（`account_credentials_redact.go:36-50`）只保留 12 个
   敏感键，`model_mapping` 不在其中——漏传即白名单被清空，账号会开始接受一切模型。
   `extra` 同理（只保留 quota_*/upstream_billing_probe*/ollama_cloud_usage* 白名单）。
2. **回滚绝不能发 `group_ids: []`**：该分支删完绑定后**跳过** SchedulerOutbox 刷新事件
   （`account_repo.go:1786-1791`），调度器快照不更新。要发原始 id 列表。

## 7. 分组16 结论：遗留垃圾

- 建于 2026-07-13 18:26，`created_at == updated_at`，此后从未被修改过。
- `account_groups` 中 **0 条**绑定（含软删账号也为 0）——不是"账号都挂了"，是**从来没绑过**。
- `usage_logs` 中该组**零条记录**，即从未成功服务过任何请求。
- 所谓「7 天 503 共 80 次」实为 2026-07-23 21:19–21:30 单个用户 11 分钟内的集中报错，
  全部是 `internal / No available upstream account supports the requested model`
  ——空组的必然结果，与上游无关。
- 无任何订阅套餐（`subscription_plans`）指向组10 或组16，购买页无泄漏面。
- 2026-07-26 已置 `inactive`，对客户不可见。

**判定：建组后未完成配置即废弃的遗留垃圾，不是漏配**（漏配会留下绑定或流量痕迹）。
建议软删除以免继续污染分组列表和错误统计；保持 inactive 亦无害。删除前确认无 API Key
仍绑该组（当前为 0）。
