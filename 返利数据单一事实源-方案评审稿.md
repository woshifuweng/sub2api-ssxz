# 返利数据单一事实源 —— 方案评审稿

> 分支 `refactor/rebate-single-source`（基线 `c1b08d22b`）。本稿为**只读阶段**产出：
> 含生产偏差量化基线 + 统一方案。**未动任何写路径**，等确认后再实施。
> 配套只读审计脚本：[backend/scripts/audit-affiliate-rebate-consistency.sql](backend/scripts/audit-affiliate-rebate-consistency.sql)

## 1. 问题（已查实，不复述细节）

「累计返利」有三套互不相干的算法：

| 口径 | 定义 | 使用方 | 维护点 |
|---|---|---|---|
| **A** | `user_affiliates.aff_history_quota` 冗余列（终身累计，转余额不回冲） | 用户页 4 张统计卡 | `affiliate_repo.go` AccrueQuota 与 `usage_billing_repo.go` accrueUsageAffiliateRebate 两条 UPDATE 各自维护 |
| **B** | `SUM(user_affiliate_ledger.amount) WHERE action='accrue'` | 管理端列表/用户总览/运营看板 | 同两处的 INSERT |
| **C** | 按「现存且仍绑定」被邀人 join ledger 的 SUM | 用户页邀请记录明细 | 只读派生；受 LIMIT 100 截断，被邀人删号（`user_id` CASCADE / ledger `source_user_id` SET NULL）或解绑后从 C 消失但留在 A/B |

同类双口径：**邀请人数**（`aff_count` 计数列 vs `COUNT(DISTINCT source_user_id)`）、
**可结算额度**（裸 `aff_quota` 依赖 GetAffiliateDetail 的 lazy thaw 副作用 vs 管理端 `aff_quota + SUM(matured frozen)`）。

独立问题：`resolveRebateRatePercent` 对显式 0 的处理。注意：仓库已有测试
`TestAffiliateRebateRateZeroIsNotUnset` **锁定了「显式 0 = 该用户返利关闭」的合法语义**
（管理端也依赖 0 与 NULL 的区分），所以修复方向不是「把 0 当未设置」，而是**防意外写 0 + 让 0 可见可审计**（见 §5）。

## 2. 生产偏差基线（2026-07-26 只读审计，全量）

盘面：`user_affiliates` 14 行、绑定关系 3 条、ledger 7 行（accrue×5 计 $0.00675773，transfer×2 计 $100.00675773），action 仅 accrue/transfer 两种，无负数。

**全库偏差集中在 user_id=1（管理员）一个账号**：

| 检查项 | 结果 |
|---|---|
| A vs B（history 列 vs SUM accrue） | 仅 user 1 偏差：A=100.0，B=0，**差 100** |
| 守恒（quota+frozen vs accrue−transfer） | 仅 user 1 破坏：ledger 有 transfer 100 但**无对应 accrue 底账**，差恰为 100 |
| B vs C（join 缺口 / 孤儿流水） | 0 缺口，5 条 accrue 全部可正常 join |
| LIMIT 100 截断 | 无（最大绑定数远小于 100） |
| aff_count vs 现存绑定数 | 3 个邀请人全部一致 |
| 冻结/lazy-thaw 窗口差 | 0（全局 freeze_hours=0，无冻结存量） |
| 比例 | 全局 5%；1 人专属正值；**user 1 显式 0.00 且 history=100** |

**结论**：user 1 的 $100 是 ledger 建表（migration 131）之前直接写列的历史遗留——先有列值 100，
后来转余额记了 `transfer` 却没有 accrue 底账；它同时是「比例 0% vs 累计奖励 $100」矛盾的数据源头
（显式 0 疑似早期管理端把「未设置」误存为 0，需人工确认意图）。其余 13 行完全一致。
**即迁移成本极低：唯一需要拍板的历史修复就是这一个账号。**

## 3. 目标口径（单一事实源 = `user_affiliate_ledger`）

| 业务量 | 唯一定义（ledger 推导） | 现冗余列的角色 |
|---|---|---|
| 终身累计返利 | `SUM(accrue) + SUM(adjust)`（adjust=历史遗留补记，见裁决 1） | `aff_history_quota` 降级为缓存 |
| 冻结中 | `SUM(accrue AND frozen_until > NOW())` | `aff_frozen_quota` 降级为缓存 |
| 可结算 | `SUM(accrue) + SUM(adjust) − SUM(transfer) − 冻结中` | `aff_quota` 降级为缓存 |
| 已转余额 | `SUM(amount) FILTER (action='transfer')` | （无列，管理端已用此口径） |
| 邀请人数（语义拆分） | ①现存绑定数 = `COUNT(ua.inviter_id=X)`（**关系本身是事实源**——绑定不写 ledger，无法由流水重建；被邀人删号 CASCADE 后随之减少，语义即「现存」）；②产生过返利人数 = `COUNT(DISTINCT accrue.source_user_id)` | `aff_count` 降级为①的缓存；UI 明确标注用的是哪个语义。（可选增强，需拍板：BindInviter 补写 `action='bind'` 流水，才能提供「历史邀请总数」第三语义——不建议本期做） |
| 邀请记录明细（C） | 以 ledger 按 `source_user_id` 聚合为主表，LEFT JOIN users 取现存资料，删号显示「已注销用户」而非消失；**加分页**替代 LIMIT 100 静默截断 | — |

设计原则：
- **ledger 只增不改**（现状已是 append-only，转余额快照列 134 已就位）；
- 冗余列保留（登录态快速读、admin 列表排序需要索引列），但**降级为「可由 ledger 全量重建的投影」**，任何口径争议以 ledger 为准；
- 双写保持现状（两处写点本就同事务写列+写 ledger），**不在本期拆双写**——拆了反而引入读放大风险；本期只是把「谁是真相」定死并加校验闭环。

## 4. 实施分期（P1 起每步都需你确认后执行）

**P0（本次已完成，只读）**：审计脚本 + 生产偏差基线（§2）。

**P1 一致性校验闭环（新增代码，不改现有写路径）**
- 【已裁决：做纯 SQL 运维脚本，不做 admin 端点】`backend/scripts/affiliate-reconcile-guard.sh`，与 billing-reconcile-guard.sh 同范式（基线文件 + 只对残差**位移**告警 + 退出码 0/1/2），避免 gin+gateway 双注册成本与新增鉴权面；真到需要持续盯的那天再提升为端点；
- 固定校验的三条不变量：① `aff_history_quota == SUM(accrue)`；② `aff_quota + aff_frozen_quota == SUM(accrue) − SUM(transfer)`；③ `aff_count == COUNT(inviter_id=X)`（管理端列表页同屏可见 ①②，运营可直接目检）；
- 单测覆盖推导公式（`go test -tags unit ./...`，注意该命令同时选中 unit 标签与无标签测试）；
- 验收：生产跑 reconcile，除 user 1 外 0 偏差。

**P2 读路径统一切 ledger**
- 用户页 `GetAffiliateDetail`：`aff_history_quota`/`aff_quota`/`aff_frozen_quota`/`aff_count` 改为 ledger 推导值（一条聚合 SQL，管理端 `affiliateUserOverviewSQL` 已是半成品可复用）；lazy thaw 保留但不再影响展示口径（展示不依赖 thaw 是否跑过）；
- 邀请记录明细改 ledger 驱动 + 分页；删号被邀人显示占位；
- 管理端列表 `ListUsersWithCustomSettings` 的 `aff_history_quota` 列改用已在查询里的 `accrued_rebate_total`（B），列值仅剩排序用途；
- 验收：同一用户在用户页/管理端/运营看板三处数字一致。

**P3 历史修复 + 列重建（一次性数据操作，先备份）**
- **user 1 的 100 底账**，三选一（需拍板）：
  - **方案甲（推荐）**：补一条 `action='adjust'` 的 ledger 底账（amount=100，备注历史迁移），守恒公式扩展为 `accrue+adjust−transfer`——账实相符且不污染 accrue 统计；
  - 方案乙：补一条 `source_user_id=NULL` 的 accrue——简单但会把 100 计入「累计返利」统计口径；
  - 方案丙：不补，接受重建后管理员历史清零（展示从 100→0）；
- user 1 的 `aff_rebate_rate_percent=0`：人工确认意图，若为历史误写则 `UPDATE ... SET aff_rebate_rate_percent = NULL`；
- 全表列重建：`UPDATE user_affiliates SET 列 = ledger 推导值`（迁移文件 `199_rebate_ledger_single_source.sql` 起；遵守迁移硬规矩：**131/132/133 已进 checksum 兼容白名单、绝不可再改**，新文件必须幂等 IF EXISTS/IF NOT EXISTS，CONCURRENTLY 索引单独放 `*_notx.sql`，只进不退；`aff_count` 不参与 ledger 重建，用 `COUNT(inviter_id)` 关系重建）；
- 验收：reconcile 全绿（0 偏差）。

**P4（后续、可选，不在本期）**：拆双写、`aff_rebate_disabled` 布尔列拆「0=关闭」语义。

**边界**：`usage_billing_repo.go` 属发布线收尾范围，本方案**全程不改它**——其现有双写行为与方案兼容
（同事务写列+写 ledger，P2/P3 均不需要动它）；P4 若要拆双写才会涉及，届时另行评审。

## 5. 比例显式 0 的防误写（保语义、堵来源、可审计）

1. **保留** 0=关闭 语义（测试已锁定，管理端依赖）；
2. `AdminSetUserRebateRate`/`AdminBatchSetUserRebateRate` 收到 `ratePercent=0` 时写 admin_audit_logs（197 迁移已有审计表），管理端 UI 提交 0 时二次确认文案「将关闭该用户返利」；
3. reconcile 报告固定输出「rate=0 且 history>0」的用户清单（当前命中 user 1，正是本次三处矛盾之一的根源）；
4. 全局比例路径不变：`AffiliateRebateRateMin=0` 保留（全局设 0 = 全站关闭返利，是合法运营操作）。

## 6. 风险与回滚

| 风险 | 缓解 |
|---|---|
| **运营看板分桶依赖 thaw 副作用**：`dashboard_operations_repo.go` 的「可结算」桶按 ledger `updated_at` 归窗，靠解冻时 `SET frozen_until=NULL, updated_at=NOW()` 驱动 | P2/P3 必须保留 thaw 对 ledger `updated_at` 的 bump（或同步改造该看板 SQL）——已列入 P2 验收项 |
| P2 聚合读性能（ledger 无 per-user 汇总索引压力） | 现有 `idx_ual_frozen_thaw` + `(action,...)` 部分索引已覆盖；数据量 7 行起步，量级到 10^6 前无压力；必要时 P4 引入物化 |
| P3 列重建把「故意的手工调整」抹掉 | 重建前 reconcile 报告人工过目；备份 `pg_dump -t user_affiliates -t user_affiliate_ledger` |
| 迁移编号冲突（上游同号） | 按仓库规范完整文件名记账，编号重复不冲突；用 199 起的自定义段 |
| 显式 0 语义误伤 | 不改判 0，只加审计与确认（§5） |

## 附录 A：代码读写点全量清单（多智能体盘点，4 agent / 127 次检索，零遗漏为目标）

### A.1 冗余列写点（全部 8 处，两两成对「列 UPDATE + ledger INSERT」除标注外）

| 文件:行 | 列 | 说明 |
|---|---|---|
| `affiliate_repo.go:103` | aff_count | BindInviter +1；**无 ledger 配对**（绑定不写流水） |
| `affiliate_repo.go:127` | aff_frozen_quota + aff_history_quota | AccrueQuota 冻结分支，配 L143 accrue INSERT（带 frozen_until/source_order_id） |
| `affiliate_repo.go:129` | aff_quota + aff_history_quota | AccrueQuota 直入分支，配 L150 accrue INSERT |
| `affiliate_repo.go:223` | aff_quota ↑ / aff_frozen_quota ↓ | thawFrozenQuotaTx，与 L197 ledger `frozen_until=NULL, updated_at=NOW()` 配对 |
| `affiliate_repo.go:259` | aff_quota → 0 | TransferQuotaToBalance，配 L312 transfer INSERT（带 4 个 *_after 快照） |
| `affiliate_repo.go:765` | （默认 0 初始化） | ensureUserAffiliateWithClient INSERT ON CONFLICT DO NOTHING |
| `usage_billing_repo.go:305` | aff_quota\|aff_frozen_quota（**动态拼列名**，grep 字面 SQL 抓不到）+ aff_history_quota | 用量计费返利，配 L325/331 accrue INSERT（**source_order_id 恒 NULL**）【发布线范围，只读不改】 |
| `affiliate_repo.go:1068/1098` | aff_rebate_rate_percent | Admin 单个/批量 设置或清除（NULL=清除） |

### A.2 关键读点（口径混用现场）

- `affiliate_repo.go:25-51` `affiliateUserOverviewSQL`（管理端用户总览）：**半 ledger 化样板**——available=列+matured ledger、rebated_invitee_count=ledger DISTINCT，但 invited_count/history 仍读列；
- `affiliate_repo.go:344-364` ListInvitees（用户页 C 口径，LIMIT 100）；`:426` 管理端邀请记录同 join；`:470` 返利记录（要求 source_order_id NOT NULL → **用量返利天然不出现在该列表**）；`:1181-1187` 管理端列表 ledger SUM（B）；
- `usage_billing_repo.go:287` 单人上限 cap 检查（ledger SUM）；`:255` 邀请关系+比例读取（FOR UPDATE）；
- `dashboard_operations_repo.go:44-76` 运营看板：纯 ledger（pending 按 created_at 归窗、available 按 updated_at 归窗——**依赖 thaw bump updated_at**）；
- `admin_user.go:787/833`：transfer 流水伪装成 `AFF-<id>` 兑换码进入管理端余额历史。

### A.3 API / 前端消费面

- 用户端唯一出口 `GET /api/v1/user/aff`（AffiliateDetail 原样透出 4 列）→ `AffiliateView.vue` 4 张统计卡 + 邀请明细表（同屏混 A 与 C）；
- 管理端 `GET /admin/affiliates/users`（列 + ledger SUM 同行混排，同屏可目检不变量 accrued == frozen+quota+transferred == history）、`/users/:id/overview`（仅 inline gin 注册，gateway 缺）、`/dashboard/operations-summary`（纯 ledger）；
- 改字段需同步的前端契约测试：`AffiliateView.spec.ts`、`AffiliatesView.spec.ts`、`api/admin/__tests__/affiliate.spec.ts`、`AdminOperationsSummary.spec.ts`、admin `DashboardView.spec.ts`；
- AffiliateRepository 接口一改即涉及的 stub：`payment_fulfillment_test.go`、`payment_public_lookup_test.go`、`auth_email_oauth_test.go` 等。

### A.4 schema / 迁移事实

- ledger 列全集与 5 个索引（131+133+134）；action 全库仅 `accrue`/`transfer` 两种（4 个 accrue INSERT 点、1 个 transfer INSERT 点）；
- `user_affiliates.user_id` FK CASCADE（删号连坐删行）、`inviter_id` SET NULL、ledger `source_user_id` SET NULL、`source_order_id` SET NULL；
- 迁移运行器：`schema_migrations(filename PK, checksum, applied_at)` SHA256 记账、advisory lock、字典序执行、只进不退；**131/132/133 在 checksum 兼容白名单上（历史上已被改过一次），绝不可再改**；新迁移取 199+。

## 附录 B：生产审计原始输出

（2026-07-26 深夜，只读事务执行，全文见会话记录；关键数字已录入 §2。）

---

## 裁决记录（2026-07-27，四条全部拍板）

1. **user 1 的 100 底账 → 选甲**：补 `action='adjust'` 台账行（+100，notes 注明由来）。
   不选乙——accrue 语义是真实产生的返利收益，伪造会让报表出现从未发生的推荐收入；
   不选丙——恒等式对 user 1 永久红，长期报红的检查等于没有检查。
   落地：[migrations/199_affiliate_ledger_adjust_action.sql](backend/migrations/199_affiliate_ledger_adjust_action.sql)（schema：notes 列 + action 语义扩展）
   + [scripts/affiliate-rebate-fix-user1.sql](backend/scripts/affiliate-rebate-fix-user1.sql)（生产点修，dry-run 先行，前置条件硬校验）。
2. **user 1 的显式 0 比例 → 判定误写，复位 NULL**。产生机制已定位：AffiliatesView.vue
   `v-model.number` + `Number('')===0`，清空输入框时静默提交显式 0%。仅动 user 1；
   其他账号显式 0 是合法业务值（既有裁决），一律不动。前端防误写另行修复（后续任务）。
3. **P1 一致性检查 → 纯 SQL 运维脚本，不做 admin 端点**（省掉 gin+gateway 双注册与新增鉴权面）。
   落地：[scripts/affiliate-reconcile-guard.sh](backend/scripts/affiliate-reconcile-guard.sh)，
   与 billing-reconcile-guard.sh 同范式：**按「残余位移」告警，不按绝对值告警**
   （基线文件 /var/lib/sub2api-guard/affiliate-residual.baseline，退出码 0/1/2）。
   三条恒等式：r_hist、r_conserv（含 adjust 项）、r_count。
4. **不补 `action='bind'` 流水**：aff_count 事实源是 inviter_id 关系，补 bind 行等于给
   一个不由台账推导的数字造第二个数据源——正是本次重构要消灭的缺陷。

执行纪律：修复脚本先 dry-run 给用户过目，确认后才 `-v apply=1` 执行；
生产除直连改库外不做任何其他动作；131-133 与落地后的 199+ 迁移文件绝不再改；
解冻必须继续 bump 台账 `updated_at`（运营看板「可结算」分桶依赖）。
